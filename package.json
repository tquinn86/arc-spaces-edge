{
  "name": "arc-spaces-edge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Arc browser-style Spaces with persistent, revertable tabs for Microsoft Edge",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/.bin/jest --verbose"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "@jest/globals": "^29.7.0"
  }
}
